<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/bg_dark">

    <!-- Top Action Bar for Overlay Launching -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:background="#1E293B"
        android:padding="8dp"
        android:gravity="center_vertical">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="PoGo Companion v2.0.0"
            android:textColor="@color/cyan_accent"
            android:textSize="15sp"
            android:textStyle="bold" />

        <Button
            android:id="@+id/btnLaunchOverlay"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="🚀 LAUNCH FLOATING PILL"
            android:textColor="@color/white"
            android:textSize="11sp"
            android:textStyle="bold"
            android:backgroundTint="@color/cyan_accent"
            android:paddingStart="12dp"
            android:paddingEnd="12dp" />
    </LinearLayout>

    <!-- Full Pokédex Dashboard WebView -->
    <WebView
        android:id="@+id/pokedexWebView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

</LinearLayout>
