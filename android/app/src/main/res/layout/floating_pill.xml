<?xml version="1.0" encoding="utf-8"?>
<!-- Text-only pill. The window width is set in FloatingOverlayService (PILL_WIDTH_DP);
     layout_width here is ignored because the view is inflated without a parent. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/pillRoot"
    android:layout_width="72dp"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:gravity="center_horizontal"
    android:background="@drawable/pill_background"
    android:paddingStart="3dp"
    android:paddingEnd="3dp"
    android:paddingTop="4dp"
    android:paddingBottom="4dp">

    <!-- Which screen was recognised: CATCH / STORAGE / STANDBY -->
    <TextView
        android:id="@+id/pillModeBadge"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="STANDBY"
        android:textColor="@color/slate_text"
        android:textSize="8sp"
        android:textStyle="bold"
        android:gravity="center"
        android:background="@drawable/badge_bg"
        android:paddingTop="1dp"
        android:paddingBottom="1dp"
        android:layout_marginBottom="2dp" />

    <!-- Detected Pokémon + CP (hidden until a scan identifies something) -->
    <TextView
        android:id="@+id/pillTargetLabel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="@color/white"
        android:textSize="9sp"
        android:textStyle="bold"
        android:gravity="center"
        android:maxLines="2"
        android:ellipsize="end"
        android:visibility="gone"
        android:layout_marginBottom="2dp" />

    <!-- 1-tap SCAN -->
    <TextView
        android:id="@+id/pillScanBtn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="SCAN"
        android:textColor="@color/white"
        android:textSize="11sp"
        android:textStyle="bold"
        android:gravity="center"
        android:background="@drawable/scan_btn_bg"
        android:paddingTop="6dp"
        android:paddingBottom="6dp"
        android:clickable="true"
        android:focusable="true"
        android:layout_marginBottom="3dp" />

    <!-- Answer 1: BERRY (catch) / VERDICT (storage) -->
    <TextView
        android:id="@+id/pillSlot1Caption"
        style="@style/PillCaption" />
    <TextView
        android:id="@+id/pillSlot1Value"
        style="@style/PillValue"
        android:text="READY" />

    <View style="@style/PillDivider" />

    <!-- Answer 2: IV CHECK -->
    <TextView
        android:id="@+id/pillSlot2Caption"
        style="@style/PillCaption" />
    <TextView
        android:id="@+id/pillSlot2Value"
        style="@style/PillValue"
        android:text="TAP SCAN" />

    <View style="@style/PillDivider" />

    <!-- Answer 3: AFTER CATCH (catch) / REASON (storage) -->
    <TextView
        android:id="@+id/pillSlot3Caption"
        style="@style/PillCaption" />
    <TextView
        android:id="@+id/pillSlot3Value"
        style="@style/PillValue"
        android:visibility="gone" />

    <View style="@style/PillDivider" />

    <!-- OPEN the full app  |  close the pill -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginTop="2dp">

        <TextView
            android:id="@+id/pillExpandBtn"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="OPEN"
            android:textColor="@color/cyan_accent"
            android:textSize="8sp"
            android:textStyle="bold"
            android:gravity="center"
            android:background="@drawable/expand_btn_bg"
            android:paddingTop="4dp"
            android:paddingBottom="4dp"
            android:layout_marginEnd="1dp"
            android:clickable="true"
            android:focusable="true" />

        <TextView
            android:id="@+id/pillCloseBtn"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="CLOSE"
            android:textColor="@color/red_delete"
            android:textSize="8sp"
            android:textStyle="bold"
            android:gravity="center"
            android:background="@drawable/expand_btn_bg"
            android:paddingTop="4dp"
            android:paddingBottom="4dp"
            android:layout_marginStart="1dp"
            android:clickable="true"
            android:focusable="true" />
    </LinearLayout>

</LinearLayout>
